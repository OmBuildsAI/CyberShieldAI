[server]
# Bind to all interfaces so the app can be accessed remotely if needed
address = "0.0.0.0"
port = 8501
enableCORS = false
enableXsrfProtection = false

[browser]
serverAddress = "localhost"
serverPort = 8501
gatherUsageStats = false
